{
  "name": "follow-builders-scripts",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build-html-digest": "node scripts/prepare-digest.js && node scripts/build-digest.js && node scripts/render-html.js",
    "start": "npm run build-html-digest && npx serve . -l 10000"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "proper-lockfile": "^4.1.0"
  }
}